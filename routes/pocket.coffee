express = require 'express'
log = require '../util/log.coffee'
util = require '../util/util.coffee'

router = express.Router()

#Get the list of tags
#Get the list of items with tag

module.exports = router